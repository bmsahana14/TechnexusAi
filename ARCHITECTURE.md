# 🏗️ TechNexus Deployment Architecture

## Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         RENDER CLOUD                             │
│                                                                  │
│  ┌────────────────────┐  ┌────────────────────┐  ┌────────────┐│
│  │   Frontend         │  │   AI Service       │  │  Realtime  ││
│  │   (Next.js)        │  │   (FastAPI)        │  │  Service   ││
│  │                    │  │                    │  │ (Socket.io)││
│  │  Port: 3000        │  │  Port: 8000        │  │ Port: 4000 ││
│  │                    │  │                    │  │            ││
│  │  technexus-quiz-   │  │  technexus-ai-     │  │ technexus- ││
│  │  platform          │  │  service           │  │ realtime-  ││
│  │  .onrender.com     │  │  .onrender.com     │  │ service    ││
│  │                    │  │                    │  │.onrender.  ││
│  └────────┬───────────┘  └─────────┬──────────┘  └─────┬──────┘│
│           │                        │                    │       │
│           │  HTTP Requests         │                    │       │
│           ├───────────────────────►│                    │       │
│           │                        │                    │       │
│           │  WebSocket Connection  │                    │       │
│           ├────────────────────────┼───────────────────►│       │
│           │                        │                    │       │
└───────────┼────────────────────────┼────────────────────┼───────┘
            │                        │                    │
            │                        │                    │
            ▼                        ▼                    ▼
    ┌───────────────┐        ┌──────────────┐    ┌──────────────┐
    │   Supabase    │        │Google Gemini │    │   Users      │
    │   Database    │        │     API      │    │ (WebSocket)  │
    └───────────────┘        └──────────────┘    └──────────────┘
```

## Service Communication Flow

### 1. Quiz Creation Flow
```
User → Frontend → AI Service → Gemini API
                     ↓
                  Quiz Data
                     ↓
              Frontend → Supabase
```

### 2. Real-time Quiz Flow
```
Host → Frontend → Realtime Service ──┐
                                      │
Participants → Frontend → Realtime ──┤
                         Service      │
                            ↓         │
                    WebSocket Broadcast
                            ↓         │
                    All Connected ────┘
                       Clients
```

## Environment Variables Flow

### Frontend Environment Variables
```
NEXT_PUBLIC_SUPABASE_URL ────────► Supabase Database
NEXT_PUBLIC_SUPABASE_ANON_KEY ───► Supabase Auth
NEXT_PUBLIC_SOCKET_URL ──────────► Realtime Service
NEXT_PUBLIC_AI_SERVICE_URL ──────► AI Service
```

### AI Service Environment Variables
```
GEMINI_API_KEY ──────────────────► Google Gemini API
PORT ────────────────────────────► Render (auto-assigned)
```

### Realtime Service Environment Variables
```
PORT ────────────────────────────► Render (auto-assigned)
```

## Deployment Steps Visualization

```
┌─────────────────────────────────────────────────────────────┐
│ Step 1: Push to GitHub                                      │
│                                                              │
│  Local Code ──git push──► GitHub Repository                 │
└──────────────────────────────────┬──────────────────────────┘
                                   │
┌──────────────────────────────────▼──────────────────────────┐
│ Step 2: Create Blueprint on Render                          │
│                                                              │
│  Render Dashboard ──connect──► GitHub Repo                  │
│                                                              │
│  Render detects render.yaml                                 │
│  └─► Creates 3 services automatically                       │
└──────────────────────────────────┬──────────────────────────┘
                                   │
┌──────────────────────────────────▼──────────────────────────┐
│ Step 3: Configure Environment Variables                     │
│                                                              │
│  AI Service:     GEMINI_API_KEY                             │
│  Frontend:       NEXT_PUBLIC_* variables                    │
│  Realtime:       (no additional vars needed)                │
└──────────────────────────────────┬──────────────────────────┘
                                   │
┌──────────────────────────────────▼──────────────────────────┐
│ Step 4: Deploy                                              │
│                                                              │
│  Render builds and deploys all 3 services                   │
│  ├─► AI Service:      pip install + uvicorn                 │
│  ├─► Realtime:        npm install + npm start               │
│  └─► Frontend:        npm install + npm build + npm start   │
└──────────────────────────────────┬──────────────────────────┘
                                   │
┌──────────────────────────────────▼──────────────────────────┐
│ Step 5: Update Frontend URLs                                │
│                                                              │
│  Get actual service URLs from Render                        │
│  Update NEXT_PUBLIC_SOCKET_URL                              │
│  Update NEXT_PUBLIC_AI_SERVICE_URL                          │
│  Frontend redeploys automatically                           │
└──────────────────────────────────┬──────────────────────────┘
                                   │
┌──────────────────────────────────▼──────────────────────────┐
│ Step 6: Test & Go Live! 🎉                                  │
│                                                              │
│  ✅ All services running                                    │
│  ✅ Real-time communication working                         │
│  ✅ AI generation functional                                │
│  ✅ Platform accessible worldwide                           │
└─────────────────────────────────────────────────────────────┘
```

## Data Flow During Quiz Session

```
┌──────────────────────────────────────────────────────────────┐
│                    Quiz Session Flow                          │
└──────────────────────────────────────────────────────────────┘

1. Host Creates Quiz
   ┌─────────┐
   │  Host   │
   └────┬────┘
        │ Upload PDF
        ▼
   ┌─────────────┐     HTTP POST      ┌──────────────┐
   │  Frontend   │ ─────────────────► │ AI Service   │
   └─────────────┘                    └──────┬───────┘
                                             │ Generate
                                             ▼
                                      ┌──────────────┐
                                      │Gemini API    │
                                      └──────┬───────┘
                                             │ Questions
                                             ▼
   ┌─────────────┐                    ┌──────────────┐
   │  Frontend   │ ◄───────────────── │ AI Service   │
   └──────┬──────┘     Quiz Data      └──────────────┘
          │
          │ Save to DB
          ▼
   ┌─────────────┐
   │  Supabase   │
   └─────────────┘

2. Host Starts Quiz
   ┌─────────┐
   │  Host   │
   └────┬────┘
        │ Start Quiz
        ▼
   ┌─────────────┐   create-room    ┌──────────────────┐
   │  Frontend   │ ────────────────► │ Realtime Service │
   └─────────────┘                   └────────┬─────────┘
                                              │
                                              │ Broadcast
                                              ▼
                                       All Connected
                                         Clients

3. Participants Join
   ┌──────────────┐
   │Participants  │
   └──────┬───────┘
          │ Scan QR / Enter Code
          ▼
   ┌─────────────┐   join-quiz     ┌──────────────────┐
   │  Frontend   │ ───────────────► │ Realtime Service │
   └─────────────┘                  └────────┬─────────┘
                                             │
                                             │ player-joined
                                             ▼
                                      ┌──────────────┐
                                      │  Host + All  │
                                      │ Participants │
                                      └──────────────┘

4. Question & Answer Flow
   ┌─────────┐
   │  Host   │ ─── next-question ──► Realtime Service
   └─────────┘                              │
                                            │ new-question
                                            ▼
                                     All Participants
                                            │
   ┌──────────────┐                         │
   │Participants  │ ◄───────────────────────┘
   └──────┬───────┘
          │ submit-answer
          ▼
   ┌──────────────────┐
   │ Realtime Service │
   └────────┬─────────┘
            │ Calculate scores
            │ Track responses
            ▼
      Update State

5. Results & Leaderboard
   ┌─────────┐
   │  Host   │ ─── reveal-results ─► Realtime Service
   └─────────┘                              │
                                            │ question-results
                                            │ + leaderboard
                                            ▼
                                     All Participants
                                            │
                                            │ Display:
                                            │ - Correct answer
                                            │ - Poll results
                                            │ - Leaderboard
                                            ▼
                                      Updated Scores
```

## Free Tier Considerations

```
┌─────────────────────────────────────────────────────────────┐
│                    Free Tier Behavior                        │
└─────────────────────────────────────────────────────────────┘

Active State (User Activity)
┌──────────────┐
│   Service    │ ──► Running normally
│   Active     │     Fast response times
└──────────────┘

After 15 minutes of inactivity
┌──────────────┐
│   Service    │ ──► Spins down
│   Sleeping   │     Saves resources
└──────────────┘

First request after sleep
┌──────────────┐
│   Service    │ ──► Cold start (30-60s)
│   Waking Up  │     Then normal operation
└──────────────┘

Solution: Upgrade to Starter ($7/month per service)
┌──────────────┐
│   Service    │ ──► Always running
│   Always On  │     No cold starts
└──────────────┘
```

## Security Considerations

```
Environment Variables (Secrets)
├─► GEMINI_API_KEY ────────► Never commit to Git
├─► SUPABASE_ANON_KEY ─────► Store in Render env vars
└─► SUPABASE_URL ──────────► Public but use with anon key

CORS Configuration
├─► AI Service ────────────► Configured for all origins (*)
├─► Realtime Service ──────► Configured for all origins (*)
└─► Production ────────────► Consider restricting to your domain

HTTPS
├─► All Render services ───► Automatic HTTPS
├─► WebSocket ─────────────► Uses WSS (secure)
└─► API Calls ─────────────► Uses HTTPS
```

## Monitoring & Logs

```
Render Dashboard
├─► Logs Tab ──────────────► Real-time logs for each service
├─► Metrics ───────────────► CPU, Memory, Request stats
├─► Events ────────────────► Deploy history, crashes
└─► Shell ─────────────────► Direct access to container
```

---

For detailed deployment instructions, see:
- **Quick Start**: [DEPLOY_TO_RENDER.md](./DEPLOY_TO_RENDER.md)
- **Detailed Guide**: [RENDER_DEPLOYMENT.md](./RENDER_DEPLOYMENT.md)
- **Checklist**: [DEPLOYMENT_CHECKLIST.md](./DEPLOYMENT_CHECKLIST.md)
